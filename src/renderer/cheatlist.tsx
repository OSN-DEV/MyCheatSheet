import React from 'react';
import ReactDOM from 'react-dom/client';
import { CheatListMain } from './CheatListMain';


ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <CheatListMain />
  </React.StrictMode>
);
